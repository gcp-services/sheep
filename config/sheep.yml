# Example and test config.
rabbitmq:
  enabled: true
  hosts:
    - amqp://localhost
    - amqp://localhost
  queue: "sheep"
  
cockroachdb:
  enabled: true

pubsub:
  enabled: false

spanner:
  enabled: false
  project: test
  instance: sheep-test
  database: test